import styled from 'styled-components/macro';
import tw from 'twin.macro';

const Label = styled.label`
    ${tw`block text-sm font-semibold text-neutral-700 dark:text-neutral-200 mb-2`}
`;

export default Label;
